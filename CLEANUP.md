# 📦 Project Structure Cleanup

Текущая структура проекта немного хаотична. Вот рекомендации по организации перед пушем на GitHub.

## 🎯 Целевая структура

```
hotpaws/
├── README.md              ✅ Корень
├── LICENSE                ✅ Корень
├── CONTRIBUTING.md        ✅ Корень
├── .gitignore            ✅ Корень
├── Info.plist            ✅ Корень (нужен для сборки)
├── AppIcon.icns          ✅ Корень (ресурс)
│
├── src/                  # Swift исходники
│   ├── main.swift       
│   ├── ConfigManager.swift
│   ├── AppDelegate.swift      ⚠️ переместить сюда
│   ├── HotkeyManager.swift    ⚠️ переместить сюда
│   ├── OverlayWindow.swift    ⚠️ переместить сюда
│   └── TerminalInjector.swift ⚠️ переместить сюда
│
├── resources/            # HTML/CSS/JS/JSON
│   ├── index.html       ✅
│   ├── script.js        ✅
│   ├── style.css        ✅
│   └── commands.json    ✅
│
├── scripts/              # Сборка и утилиты
│   ├── build.sh         ✅
│   ├── push.sh          ✅
│   ├── terminal.applescript ✅
│   ├── iterm2.applescript   ✅
│   └── warp.applescript     ✅
│
├── $/                    # Документация проекта
│   ├── project-doc.md   ✅
│   ├── tasks.md         ✅
│   └── design.png       ✅
│
└── build/                # Игнорируется git
    └── Hotpaws.app
```

## ❌ Что удалить/игнорировать

```
❌ $design/          # Дубликат $ папки
❌ $desi/            # Пустая папка
❌ css/              # Дубликат, есть resources/
❌ js/               # Дубликат, есть resources/
❌ assets/           # Пустая папка
❌ docs/             # Пустая папка  
❌ images/           # Пустая папка
❌ index.html (корень) # Дубликат, есть resources/
❌ hotpaws.xcodeproj/  # Xcode проект (опционально)
❌ .DS_Store         # Уже в .gitignore
```

## 🔧 Команды для очистки

```bash
cd /Users/ufoanima/Dev/experiments/hotpaws

# Переместить Swift файлы в src/
mv AppDelegate.swift src/
mv HotkeyManager.swift src/
mv OverlayWindow.swift src/
mv TerminalInjector.swift src/

# Удалить дубликаты и пустые папки
rm -rf $design $desi
rm -rf css js assets docs images
rm index.html  # Дубликат resources/index.html

# Опционально: удалить Xcode проект если не используешь
# rm -rf hotpaws.xcodeproj

# Проверить что получилось
ls -la
```

## 📝 Обновить build.sh

После переноса файлов, обнови `scripts/build.sh`:

```bash
# Заменить пути на:
SWIFT_FILES="src/*.swift"  # Вместо отдельных файлов
```

## ✅ Чеклист перед пушем

- [ ] Swift файлы в src/
- [ ] HTML/CSS/JS в resources/
- [ ] Удалены дубликаты
- [ ] Удалены пустые папки
- [ ] build.sh работает с новой структурой
- [ ] .gitignore актуален
- [ ] README корректен

## 🎬 После очистки

```bash
# Проверить что всё работает
./scripts/build.sh
open build/Hotpaws.app

# Если всё ОК:
git add -A
git commit -m "refactor: clean up project structure"
git push origin main
```

---

💡 **Совет**: Можешь оставить Xcode проект если планируешь использовать Xcode для разработки.
