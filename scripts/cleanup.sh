#!/bin/bash

# Hotpaws — Очистка билдов
# Удаляет собранное приложение для чистой пересборки

set -e

cd "$(dirname "$0")/.."

echo "🧹 Очистка билдов..."

rm -rf build/Hotpaws.app
rm -rf build/*.app
rm -f build/.DS_Store

echo "✅ Готово! Запустите ./scripts/build.sh для пересборки"
