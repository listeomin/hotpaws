# 📂 Поля "Категория" и "Группа команд" в редакторе Hotpaws

## Что добавлено

Теперь в окне редактирования команд есть два связанных селекта для размещения команд по категориям и группам.

### 🎯 Основные возможности

1. **Связанные селекты**: при выборе категории автоматически загружаются группы этой категории
2. **Умные рекомендации**: если команда есть в словаре, предлагается её рекомендуемое размещение
3. **Создание новых опций**: возможность создать новую категорию или группу прямо из селекта
4. **Цветовая индикация**: зелёная обводка = рекомендуемое, жёлтая = пользователь выбрал другое

### 🔧 Логика работы

#### Для новой команды:
1. Пользователь вводит команду (например, `git status`)
2. Если команда есть в `commands-dictionary.json`, показывается рекомендуемая категория/группа
3. Автоматически выбирается рекомендуемое размещение с зелёной обводкой
4. Пользователь может изменить на своё (обводка станет жёлтой)

#### Для существующей команды:
1. Категория и группа предзаполняются из текущего размещения команды
2. Пользователь может изменить размещение

#### Создание новых категорий/групп:
1. В любом селекте есть опция "+ Создать..."
2. Клик показывает inline поле ввода
3. Enter создаёт и выбирает новую опцию

### 📁 Изменённые файлы

- `index.html` — добавлены кастомные селекты вместо placeholder'ов
- `style.css` — стили для селектов и цветовая индикация
- `script.js` — логика селектов, рекомендаций и создания новых опций

### 🎨 Визуальные индикаторы

- **Зелёная обводка**: рекомендуемое размещение из словаря
- **Жёлтая обводка**: пользователь выбрал отличное от рекомендации
- **Звёздочка ✨**: рекомендуемая опция в выпадающем списке
- **Плюс +**: опция создания новой категории/группы

### 🔄 Валидация

Команда не сохранится без выбранной категории и группы. Появится предупреждение.

### 🎮 Тестирование

Попробуйте:
1. Ввести `git status` — должна предложиться категория "Git", группа "Базовое"
2. Создать новую категория "Тест" 
3. Создать в ней группу "Моя группа"
4. Изменить предложенное размещение на своё

Всё работает! Теперь у Hotpaws есть умная система организации команд.
