# 🎯 Hotpaws — Задачи для чатов

> Каждая задача автономна и содержит всё необходимое для выполнения.
> Результаты собираются в общий проект.

---

## Сводная таблица моделей

| # | Задача | Модель | Почему |
|---|--------|--------|--------|
| 1 | Структура проекта | **Opus 4.5** / Sonnet 4.5 | Доступ к файлам |
| 2 | Swift основа | **Sonnet 4** / Opus 4.1 | Чистый код |
| 3 | HTML + JS | **Sonnet 4** | Стандартная задача |
| 4 | AppleScript | **Haiku 4.5** / Sonnet 4 | Простой код |
| 5 | Связь Swift↔JS | **Opus 4.1** / Sonnet 4 | Контекст 2 файлов |
| 6 | Хоткеи + сборка | **Opus 4.5** | Терминал + файлы |
| 7 | CSS стили | **Haiku 4.5** | Простой CSS |
| 8 | ConfigManager | **Sonnet 4** | Стандартный Swift |
| 9 | Интеграция | **Opus 4.5** | Всё вместе: файлы + терминал |

### Легенда моделей

| Модель | Особенности |
|--------|-------------|
| **Opus 4.5** | Самый умный, доступ к файлам/терминалу, дорогой |
| **Sonnet 4.5** | Баланс ум/скорость, доступ к файлам |
| **Opus 4.1** | Умный, без доступа к компьютеру |
| **Sonnet 4** | Рабочая лошадка для кода |
| **Haiku 4.5** | Быстрый, дешёвый, простые задачи |

---

## Порядок выполнения

```
[Задача 1: Структура] → [Задача 2: Swift основа] → [Задача 3: HTML/JS] 
                                ↓
[Задача 4: AppleScript] ← [Задача 5: Связь Swift↔JS]
                                ↓
                        [Задача 6: Сборка]
                                ↓
                        [Задача 7: Стили]
```

Задачи 1, 4, 7 можно делать параллельно с остальными.

---

# Задача 1: Структура проекта и конфиги

## Для тебя
Нужно создать скелет проекта: папки, пустые файлы, базовый commands.json с примерами команд. Это фундамент, на который лягут остальные части.

## Промт

```
Создай структуру macOS проекта Hotpaws.

Hotpaws — это приложение-оверлей для macOS, которое показывает подсказки по терминальным командам и позволяет вставлять их в терминал одним кликом.

Нужно создать:

1. Папку проекта /Users/ufoanima/Dev/experiments/hotpaws/ со структурой:
   - src/ (Swift файлы)
   - resources/ (HTML, JS, CSS, JSON)
   - build/ (сюда будет собираться .app)
   - scripts/ (скрипты сборки)

2. Файл resources/commands.json с примерами команд:
   - Категория "Git" с группами: Базовое, Ветки, Удалённые
   - Категория "Файлы" с группами: Навигация, Поиск
   - Категория "Процессы" с группами: Просмотр, Управление
   - По 3-5 команд в каждой группе
   - Формат:
     {
       "version": "1.0",
       "categories": [
         {
           "id": "string",
           "name": "string", 
           "icon": "emoji",
           "groups": [
             {
               "name": "string",
               "commands": [
                 {"label": "string", "command": "string", "description": "string"}
               ]
             }
           ]
         }
       ]
     }

3. Пустые файлы-заглушки:
   - src/main.swift (комментарий "// Entry point")
   - src/ConfigManager.swift (комментарий "// Config loading")
   - resources/index.html (базовый HTML5)
   - resources/script.js (пустой)
   - resources/style.css (пустой)
   - scripts/build.sh (пустой, chmod +x)

4. Файл Info.plist для macOS приложения:
   - Bundle identifier: com.hotpaws.app
   - Название: Hotpaws
   - Версия: 0.1.0
   - LSUIElement: true (без иконки в доке)
   - NSAppleEventsUsageDescription для AppleScript
```

## Модель
**Claude Opus 4.5** или **Sonnet 4.5** (нужен доступ к файловой системе для создания папок и файлов)

## Файлы для работы
- Нет, создаём с нуля

## Результат
Готовая структура папок и файлов в `/Users/ufoanima/Dev/experiments/hotpaws/`

---

# Задача 2: Swift — основа приложения

## Для тебя
Создаём главный Swift файл: AppDelegate, создание окна, базовая настройка WKWebView. Пока без горячих клавиш и без связи с JS — просто окно, которое показывает HTML.

## Промт

```
Напиши main.swift для macOS приложения Hotpaws.

Контекст: Hotpaws — приложение без окна в доке (LSUIElement), которое показывает полноэкранный полупрозрачный оверлей с WKWebView.

Требования:

1. AppDelegate:
   - Создать NSWindow на весь экран
   - Окно без заголовка, полупрозрачное (NSWindow.StyleMask: borderless)
   - Уровень окна: floating (поверх других)
   - Фон окна: прозрачный (window.isOpaque = false, backgroundColor = .clear)

2. WKWebView:
   - Занимает всё окно
   - Прозрачный фон (для CSS-стилизации)
   - Загружает index.html из bundle (пока заглушка)
   - Конфигурация для JavaScript

3. Методы:
   - showOverlay() — показать окно
   - hideOverlay() — скрыть окно
   - toggleOverlay() — переключить

4. Временно для тестирования:
   - При запуске сразу показывать окно
   - Через 3 секунды скрывать (чтобы видеть что работает)

Код должен компилироваться через swiftc без Xcode.
Используй только стандартные фреймворки: Cocoa, WebKit.

Формат ответа: только код с комментариями, без объяснений.
```

## Модель
**Claude Sonnet 4** или **Opus 4.1** (чистая генерация кода, доступ к файлам не нужен)

## Файлы для работы
- Нет

## Результат
Файл `main.swift` — скопировать в `/Users/ufoanima/Dev/experiments/hotpaws/src/main.swift`

---

# Задача 3: HTML + JavaScript интерфейс

## Для тебя
Создаём визуальную часть: HTML структура оверлея и JavaScript для рендера команд из JSON. Пока без связи со Swift — просто красивый интерфейс.

## Промт

```
Создай index.html и script.js для оверлея приложения Hotpaws.

Контекст: Это интерфейс внутри WKWebView. Показывает категории команд табами сверху, команды — карточками внизу. При клике на команду — отправляет её в Swift (пока заглушка).

index.html:
1. Структура:
   - Контейнер на весь экран с полупрозрачным фоном
   - Верхняя панель с табами категорий (горизонтальный скролл если много)
   - Основная область с группами и командами
   - Подвал с подсказкой "ESC — закрыть"

2. Семантика:
   - Табы: <nav class="categories">
   - Группа: <section class="group">
   - Команда: <button class="command">

script.js:
1. Загрузка данных:
   - Функция loadCommands(json) — принимает объект команд
   - Рендерит табы категорий
   - Рендерит команды активной категории

2. Взаимодействие:
   - Клик по табу — переключает категорию
   - Клик по команде — вызывает executeCommand(cmd)
   - executeCommand пока просто console.log + заглушка для webkit.messageHandlers

3. Навигация:
   - Стрелки ← → переключают категории
   - ESC вызывает closeOverlay()

4. Тестовые данные:
   - Внизу файла: вызов loadCommands с тестовым JSON (2 категории, по 2 группы)

Код должен работать без сборщиков, чистый ES6+.
CSS не включай — будет отдельный файл.
```

## Модель
**Claude Sonnet 4** (стандартная задача, доступ к файлам не нужен)

## Файлы для работы
- Схема commands.json из Задачи 1 (или из документации)

## Результат
Файлы `index.html` и `script.js` — скопировать в `/Users/ufoanima/Dev/experiments/hotpaws/resources/`

---

# Задача 4: AppleScript для терминалов

## Для тебя
Пишем AppleScript команды для вставки текста в разные терминалы. Это отдельный модуль, который потом вызовется из Swift.

## Промт

```
Напиши AppleScript скрипты для вставки команды в терминал macOS.

Нужны варианты для:

1. Terminal.app (встроенный)
   - Если нет открытых окон — создать новое
   - Если есть — использовать текущее
   - Вставить текст БЕЗ выполнения (пользователь сам нажмёт Enter)

2. iTerm2
   - Аналогично: текущая сессия или новая
   - Вставить текст без выполнения

3. Warp (если знаешь как)
   
4. Универсальный fallback
   - Через System Events: вставить текст в активное приложение
   - keystroke для вставки

Для каждого варианта:
- Рабочий AppleScript код
- Как вызвать из Swift через NSAppleScript или Process

Формат: код + короткий комментарий что делает.
```

## Модель
**Claude Haiku 4.5** или **Sonnet 4** (простой код, быстрая задача)

## Файлы для работы
- Нет

## Результат
Документ с AppleScript кодом — использовать в Задаче 5

---

# Задача 5: Связь Swift ↔ JavaScript

## Для тебя
Соединяем Swift и JavaScript: настраиваем WKScriptMessageHandler чтобы JS мог отправлять команды в Swift, а Swift выполнял их через AppleScript.

## Промт

```
Дополни main.swift связью между WKWebView JavaScript и Swift.

Контекст: Hotpaws — macOS приложение с WKWebView. JavaScript отправляет команды, Swift выполняет их в терминале.

Текущий код main.swift:
[ВСТАВИТЬ КОД ИЗ ЗАДАЧИ 2]

Нужно добавить:

1. WKScriptMessageHandler для сообщений из JS:
   - "executeCommand" — получает {command: "string"}, выполняет в терминале
   - "closeOverlay" — скрывает окно

2. Метод executeInTerminal(command: String):
   - Использует NSAppleScript
   - Пока только Terminal.app (потом добавим выбор)
   - AppleScript код:
     [ВСТАВИТЬ КОД ИЗ ЗАДАЧИ 4 для Terminal.app]

3. Регистрация хендлеров в WKWebView configuration:
   - userContentController.add(self, name: "executeCommand")
   - userContentController.add(self, name: "closeOverlay")

4. После выполнения команды:
   - Скрыть оверлей
   - Активировать Terminal.app

Формат: полный обновлённый main.swift с комментариями.
```

## Модель
**Claude Opus 4.1** или **Sonnet 4** (нужно понимать контекст двух файлов, но доступ к компьютеру не требуется)

## Файлы для работы
- Результат Задачи 2 (main.swift)
- Результат Задачи 4 (AppleScript для Terminal.app)

## Результат
Обновлённый `main.swift`

---

# Задача 6: Горячие клавиши и сборка

## Для тебя
Добавляем глобальную горячую клавишу F19 и пишем скрипт сборки. После этого приложение можно будет запустить!

## Промт

```
Дополни main.swift глобальной горячей клавишей и напиши build.sh.

Контекст: Hotpaws — macOS приложение. Нужно добавить глобальный хоткей F19 для показа/скрытия оверлея.

Текущий код main.swift:
[ВСТАВИТЬ АКТУАЛЬНЫЙ КОД]

Часть 1 — Горячие клавиши:

1. Использовать Carbon framework (CGEvent или hotkey API)
2. Зарегистрировать F19 как глобальный хоткей
3. При нажатии — вызывать toggleOverlay()
4. Хоткей должен работать когда приложение в фоне

Часть 2 — build.sh:

1. Скрипт сборки без Xcode:
   - Компиляция Swift файлов из src/
   - Создание .app bundle в build/
   - Копирование ресурсов (HTML, JS, CSS, JSON) в .app/Contents/Resources
   - Копирование Info.plist в .app/Contents
   - chmod +x для исполняемого файла

2. Структура .app bundle:
   hotpaws.app/
   ├── Contents/
   │   ├── Info.plist
   │   ├── MacOS/
   │   │   └── hotpaws (исполняемый)
   │   └── Resources/
   │       ├── index.html
   │       ├── script.js
   │       ├── style.css
   │       └── commands.json

3. В конце скрипта: echo с инструкцией как запустить

Формат: обновлённый main.swift + отдельно build.sh
```

## Модель
**Claude Opus 4.5** (нужен доступ к терминалу для проверки сборки и записи файлов)

## Файлы для работы
- Актуальный main.swift из Задачи 5
- Info.plist из Задачи 1

## Результат
- Обновлённый `main.swift` с горячими клавишами
- Файл `build.sh` в `/Users/ufoanima/Dev/experiments/hotpaws/scripts/`

---

# Задача 7: CSS стили оверлея

## Для тебя
Делаем красивый дизайн! Можно делать параллельно с другими задачами. Это чистый CSS без зависимостей.

## Промт

```
Создай style.css для оверлея приложения Hotpaws.

Контекст: Полноэкранный оверлей с командами для терминала. Тёмная тема, современный минималистичный дизайн.

HTML структура (из index.html):
- .overlay — контейнер на весь экран
- .categories — панель с табами
- .category-tab — отдельный таб
- .category-tab.active — активный таб
- .content — область с командами
- .group — группа команд
- .group-title — заголовок группы
- .commands — сетка команд
- .command — кнопка команды
- .command-label — название команды
- .command-code — сама команда (моноширинный)
- .footer — подвал с подсказками

Требования:

1. CSS переменные в :root для кастомизации:
   - Цвета фона, текста, акцентов
   - Размеры шрифтов
   - Отступы и скругления
   - Прозрачность

2. Оверлей:
   - Полупрозрачный тёмный фон с blur (backdrop-filter)
   - Центрирование контента
   - Плавное появление (opacity transition)

3. Табы категорий:
   - Горизонтальный ряд с gap
   - Активный таб — яркий акцент
   - Hover эффект

4. Команды:
   - Сетка карточек (CSS Grid, auto-fill)
   - Карточка: тёмный фон, светлая рамка при hover
   - Название команды — обычный текст
   - Код команды — моноширинный, чуть приглушённый

5. Анимации:
   - Hover на карточках — подсветка рамки
   - Клик — scale down
   - Переключение категорий — fade

6. Адаптивность:
   - Работает на разных размерах экрана
   - Команды перестраиваются в сетке

Цветовая схема:
- Фон: почти чёрный с 90% прозрачностью
- Текст: белый/светло-серый  
- Акцент: жёлтый (#FFDD00) или на твой выбор
- Код: приглушённый cyan

Формат: только CSS код с комментариями к секциям.
```

## Модель
**Claude Haiku 4.5** (простая задача, чистый CSS, быстро)

## Файлы для работы
- HTML структура из Задачи 3 (или описание выше)

## Результат
Файл `style.css` — скопировать в `/Users/ufoanima/Dev/experiments/hotpaws/resources/`

---

# Задача 8: ConfigManager — загрузка пользовательских конфигов

## Для тебя
Создаём модуль для работы с файлами из ~/.hotpaws/. При первом запуске копирует дефолтные конфиги, потом читает пользовательские.

## Промт

```
Напиши ConfigManager.swift для приложения Hotpaws.

Контекст: Hotpaws хранит пользовательские конфиги в ~/.hotpaws/. При первом запуске создаёт папку и копирует дефолтные файлы из bundle.

Требования:

1. Структура ConfigManager:
   - static let shared (singleton)
   - let configDirectory: URL (~/.hotpaws/)
   - let bundleResourcesURL: URL (из bundle приложения)

2. Методы:

   setupConfigDirectory():
   - Создать ~/.hotpaws/ если не существует
   - Скопировать commands.json и style.css из bundle если их нет
   - Не перезаписывать существующие файлы!

   loadCommands() -> Data?:
   - Читает ~/.hotpaws/commands.json
   - Возвращает Data для парсинга в JS или Swift

   loadStyles() -> String?:
   - Читает ~/.hotpaws/style.css  
   - Возвращает CSS как строку

   getResourcePath(_ filename: String) -> URL?:
   - Возвращает путь к файлу: сначала ищет в ~/.hotpaws/, потом в bundle

3. Обработка ошибок:
   - Логирование через print (потом заменим на os_log)
   - Fallback на bundle ресурсы если пользовательские битые

4. Использование в main.swift:
   - При старте: ConfigManager.shared.setupConfigDirectory()
   - При загрузке WebView: использовать loadStyles() для инъекции CSS

Формат: полный код ConfigManager.swift с комментариями.
```

## Модель
**Claude Sonnet 4** (стандартный Swift код, доступ к файлам не нужен)

## Файлы для работы
- Нет

## Результат
Файл `ConfigManager.swift` — скопировать в `/Users/ufoanima/Dev/experiments/hotpaws/src/`

---

# Задача 9: Интеграция и тестирование

## Для тебя
Финальная сборка: соединяем все части, исправляем баги, проверяем что всё работает.

## Промт

```
Помоги интегрировать и протестировать приложение Hotpaws.

У меня есть:
- main.swift [ВСТАВИТЬ]
- ConfigManager.swift [ВСТАВИТЬ]  
- index.html [ВСТАВИТЬ]
- script.js [ВСТАВИТЬ]
- style.css [ВСТАВИТЬ]
- commands.json [ВСТАВИТЬ]
- build.sh [ВСТАВИТЬ]
- Info.plist [ВСТАВИТЬ]

Нужно:

1. Проверить совместимость:
   - JS отправляет сообщения с правильными именами?
   - Swift слушает правильные хендлеры?
   - CSS классы совпадают с HTML?
   - JSON загружается и парсится?

2. Добавить недостающие связи:
   - ConfigManager вызывается в main.swift?
   - Стили инжектятся в WebView?
   - Команды загружаются из JSON?

3. Проверить build.sh:
   - Все файлы копируются?
   - Пути правильные?

4. Создать чеклист тестирования:
   - [ ] Приложение запускается
   - [ ] F19 показывает оверлей
   - [ ] ESC скрывает оверлей
   - [ ] Табы переключаются
   - [ ] Клик по команде отправляет в терминал
   - [ ] Стили из ~/.hotpaws/style.css применяются

Собери и запусти приложение, покажи что работает и что нет.
```

## Модель
**Claude Opus 4.5** (сложная задача: нужен доступ к файлам, терминалу, понимание всего контекста)

## Файлы для работы
- Все файлы из предыдущих задач

## Результат
- Исправленные файлы
- Работающее приложение
- Список оставшихся багов

---

# Быстрый старт: минимальный MVP

Если хочешь быстрее увидеть результат, делай задачи в таком порядке:

1. **Задача 1** — структура (5 мин)
2. **Задача 2** — Swift основа (10 мин)
3. **Задача 3** — HTML/JS (15 мин)
4. **Задача 7** — CSS (10 мин)
5. **Задача 4** — AppleScript (5 мин)
6. **Задача 5** — связь Swift↔JS (10 мин)
7. **Задача 6** — хоткеи и сборка (10 мин)

Итого: ~1 час до рабочего прототипа.

Задачи 8 и 9 — полировка, можно потом.

---

#hotpaws #tasks #prompts
