# 🎯 Hotpaws — Режим редактирования: Задачи

> Декомпозиция спецификации GUI редактирования команд.
> Каждая задача атомарна и содержит всё необходимое для выполнения.

---

## Сводная таблица

| # | Задача | Модель | Сложность | Фаза |
|---|--------|--------|-----------|------|
| 0 | Словарь терминальных команд | Haiku 4.5 | 🟢 Легко | Подготовка |
| 1 | Вход/выход из режима редактирования | Sonnet 4 | 🟢 Легко | 1 |
| 2 | Модальное окно редактирования команды | Sonnet 4 | 🟡 Средне | 1 |
| 3 | Поле "Команда" с автодополнением | Sonnet 4 | 🟡 Средне | 1 |
| 4 | Поля "Категория" и "Группа команд" (селекты) | Sonnet 4 | 🟡 Средне | 1 |
| 5 | Визуальная индикация (жёлтый/зелёный бордер) | Haiku 4.5 | 🟢 Легко | 1 |
| 6 | Кнопки "Сохранить" и "Удалить" | Sonnet 4 | 🟢 Легко | 1 |
| 7 | Логика закрытия окна (с проверкой изменений) | Sonnet 4 | 🟡 Средне | 1 |
| 8 | Создание новой команды | Sonnet 4 | 🟡 Средне | 1 |
| 9 | Inline-редактирование названий категорий | Sonnet 4 | 🟡 Средне | 2 |
| 10 | Inline-редактирование названий групп | Sonnet 4 | 🟢 Легко | 2 |
| 11 | Создание новой категории ("+ Категория") | Sonnet 4 | 🟢 Легко | 2 |
| 12 | Drag & Drop команд | Opus 4.1 | 🔴 Сложно | 2 |
| 13 | Drag & Drop категорий и групп | Opus 4.1 | 🔴 Сложно | 2 |
| 14 | Автосохранение профиля | Sonnet 4 | 🟡 Средне | 2 |
| 15 | Генерация файла aliases.sh | Sonnet 4 | 🟡 Средне | 3 |
| 16 | Интеграция с .zshrc/.bashrc | Opus 4.5 | 🔴 Сложно | 3 |
| 17 | Поле "Alias" в окне редактирования | Sonnet 4 | 🟡 Средне | 3 |
| 18 | Регистрация глобальных хоткеев команд | Opus 4.5 | 🔴 Сложно | 4 |
| 19 | Поле "Горячая клавиша" (захват + валидация) | Opus 4.1 | 🔴 Сложно | 4 |
| 20 | Выполнение команды по глобальному хоткею | Opus 4.5 | 🟡 Средне | 4 |

---

## Фаза 0: Подготовка

---

### Задача 0: Словарь терминальных команд

**Для тебя:**
Нужно создать структурированную базу терминальных команд с описаниями, категориями и группами. Эта база будет использоваться для автодополнения при вводе команд и для предложения категории/группы при создании новой команды.

**Сложность:** 🟢 Легко

**Промт:**

```
Создай JSON-файл commands-dictionary.json — словарь терминальных команд для macOS.

Контекст: Это база знаний для приложения Hotpaws. Используется для:
1. Автодополнения при вводе команды
2. Автозаполнения описания команды
3. Предложения категории и группы для новой команды

Структура файла:
{
  "version": "1.0",
  "commands": [
    {
      "command": "git status",
      "description": "Показать состояние репозитория",
      "category": "Git",
      "group": "Базовое",
      "aliases": ["gs"]  // популярные алиасы, опционально
    }
  ]
}

Требования:

1. Включи команды из этих категорий:
   - Git (базовое, ветки, удалённые репозитории, история, stash, теги)
   - Файловая система (навигация, просмотр, поиск, права, архивы)
   - Процессы (просмотр, управление, мониторинг)
   - Сеть (диагностика, загрузка, SSH)
   - Система (информация, диски, пользователи)
   - Пакетные менеджеры (Homebrew, npm, pip)
   - Docker (образы, контейнеры, compose)
   - Текст (поиск, обработка, редакторы)

2. Для каждой команды:
   - Полная команда с типичными флагами (например "ls -la", не просто "ls")
   - Краткое описание на русском (до 50 символов)
   - Категория и группа для размещения

3. Приоритет:
   - Самые частые команды для новичков
   - Команды которые сложно запомнить
   - Команды с полезными флагами

4. Количество: 150-200 команд

Источник для вдохновения (не копировать напрямую, там мусор):
- Популярные git-алиасы из oh-my-zsh
- Стандартные Unix-команды
- Команды Homebrew, npm, Docker

Формат: только JSON, без комментариев.
```

**Модель:** Claude Haiku 4.5

**Файлы для работы:**
- /Users/ufoanima/Dev/#assets/all_commands.txt (для вдохновения)

**Результат:**
Файл `commands-dictionary.json` → `/Users/ufoanima/Dev/experiments/hotpaws/resources/`

---

## Фаза 1: Базовое редактирование команд

---

### Задача 1: Вход/выход из режима редактирования

**Для тебя:**
Добавляем возможность войти в режим редактирования через CMD+E (когда оверлей открыт) или через меню "Настройки → Редактировать команды". В режиме редактирования интерфейс визуально меняется, появляется возможность кликать на команды для редактирования.

**Сложность:** 🟢 Легко

**Промт:**

```
Добавь режим редактирования в Hotpaws.

Контекст: Hotpaws — macOS приложение с WKWebView оверлеем. Нужно добавить режим редактирования команд.

Текущие файлы:
- main.swift: [ВСТАВИТЬ]
- script.js: [ВСТАВИТЬ]
- style.css: [ВСТАВИТЬ]

Требования:

1. Вход в режим редактирования:
   - CMD+E когда оверлей открыт (JS слушает keydown)
   - Пункт меню "Настройки → Редактировать команды" (Swift NSMenu)
   - При входе из меню — сначала показать оверлей, потом включить режим

2. JavaScript (script.js):
   - Глобальная переменная let isEditMode = false
   - Функция enterEditMode() — включает режим
   - Функция exitEditMode() — выключает режим
   - При входе: добавить класс .edit-mode на .overlay
   - При выходе: убрать класс

3. Визуальные изменения в режиме редактирования (CSS):
   - Индикатор режима: плашка "Режим редактирования" в верхнем правом углу
   - Курсор pointer на всех командах
   - Лёгкая пульсация или другой visual cue что режим активен

4. Выход из режима:
   - ESC — выход из режима редактирования (не закрытие оверлея!)
   - Повторный CMD+E — выход из режима
   - Если нажать ESC в обычном режиме — закрыть оверлей как раньше

5. Swift (main.swift):
   - Добавить пункт меню
   - При клике — отправить сообщение в JS через evaluateJavaScript

Формат: обновлённые файлы main.swift, script.js, style.css с комментариями к изменениям.
```

**Модель:** Claude Sonnet 4

**Файлы для работы:**
- main.swift
- script.js  
- style.css

**Результат:**
Обновлённые файлы с режимом редактирования

---

### Задача 2: Модальное окно редактирования команды

**Для тебя:**
Создаём модальное окно, которое появляется при клике на команду в режиме редактирования. Пока только структура и базовые поля: "Команда", "Описание". Остальные поля добавим в следующих задачах.

**Сложность:** 🟡 Средне

**Промт:**

```
Создай модальное окно редактирования команды для Hotpaws.

Контекст: В режиме редактирования при клике на команду должно открываться окно редактирования.

Текущий script.js: [ВСТАВИТЬ]
Текущий style.css: [ВСТАВИТЬ]
Текущий index.html: [ВСТАВИТЬ]

Требования:

1. HTML структура модального окна:
   <div class="modal-overlay">
     <div class="modal">
       <div class="modal-header">Редактирование команды</div>
       <div class="modal-body">
         <!-- поля формы -->
       </div>
       <div class="modal-footer">
         <!-- кнопки -->
       </div>
     </div>
   </div>

2. Поля формы (пока базовые):
   - "Команда" — input type="text", обязательное
   - "Описание" — textarea, опциональное
   - Placeholder для будущих полей (категория, группа, алиас, хоткей)

3. JavaScript:
   - openCommandEditor(commandData) — открыть окно с данными команды
   - closeCommandEditor() — закрыть окно
   - При клике на команду в edit mode → openCommandEditor
   - commandData = { id, label, command, description, categoryId, groupName }

4. Стили модального окна:
   - Центрирование по экрану
   - Тёмный полупрозрачный backdrop
   - Карточка с скруглёнными углами
   - Поля ввода в стиле основного интерфейса
   - Активное поле: жёлтая обводка 2px

5. Анимации:
   - Появление: fade in + небольшой scale
   - Закрытие: fade out

Формат: обновлённые index.html, script.js, style.css.
```

**Модель:** Claude Sonnet 4

**Файлы для работы:**
- index.html
- script.js
- style.css

**Результат:**
Модальное окно с базовыми полями

---

### Задача 3: Поле "Команда" с автодополнением

**Для тебя:**
Добавляем умное автодополнение в поле "Команда". При вводе показываем подсказки из словаря команд. Подсказка отображается полупрозрачно справа от введённого текста. Стрелками вверх/вниз можно листать варианты, Tab — принять подсказку.

**Сложность:** 🟡 Средне

**Промт:**

```
Добавь автодополнение в поле "Команда" в окне редактирования Hotpaws.

Контекст: При вводе команды показываем подсказки из словаря. Пользователь пишет "g" — справа полупрозрачно появляется "it" (вместе читается "git"). Стрелками можно листать варианты.

Текущий script.js: [ВСТАВИТЬ]
Текущий style.css: [ВСТАВИТЬ]
Файл словаря commands-dictionary.json: [ВСТАВИТЬ или путь]

Требования:

1. Загрузка словаря:
   - При старте загрузить commands-dictionary.json
   - Хранить в глобальной переменной commandsDictionary

2. Логика автодополнения:
   - При каждом вводе в поле "Команда" — искать совпадения
   - Поиск по началу строки (startsWith), case-insensitive
   - Показывать топ-10 совпадений
   - Текущий выбранный вариант — индекс в массиве

3. Отображение подсказки:
   - Inline-подсказка: введённый текст + полупрозрачное продолжение
   - Реализация: div с двумя span внутри input-контейнера
   - Или: использовать CSS ::after с content

4. Навигация:
   - ↑↓ — переключение между вариантами
   - Tab или → — принять подсказку
   - Esc — скрыть подсказки
   - Enter — оставить как есть (не принимать подсказку)

5. Визуал:
   - Подсказка того же шрифта, но opacity: 0.4
   - При переключении — плавная смена (transition)

6. Автозаполнение описания:
   - Когда команда совпадает со словарём полностью — заполнить поле "Описание"
   - Только если поле "Описание" пустое

Формат: обновлённые script.js и style.css.
```

**Модель:** Claude Sonnet 4

**Файлы для работы:**
- script.js
- style.css
- commands-dictionary.json

**Результат:**
Работающее автодополнение команд

---

### Задача 4: Поля "Категория" и "Группа команд" (селекты)

**Для тебя:**
Добавляем два связанных селекта: "Категория" и "Группа команд". При выборе категории — обновляется список групп. В конце каждого списка пункт "+ Создать новую". При редактировании существующей команды поля предзаполнены. При создании новой — предлагаем категорию/группу из словаря.

**Сложность:** 🟡 Средне

**Промт:**

```
Добавь поля "Категория" и "Группа команд" в окно редактирования Hotpaws.

Контекст: Два связанных селекта. При создании новой команды — предлагаем категорию из словаря. Всегда есть возможность создать новую категорию/группу.

Текущий script.js: [ВСТАВИТЬ]
Текущий style.css: [ВСТАВИТЬ]
Текущий commands.json (пользовательские команды): [ВСТАВИТЬ]
Файл commands-dictionary.json: [ВСТАВИТЬ]

Требования:

1. HTML структура:
   - Select "Категория" с опциями из commands.json + "+ Создать категорию"
   - Select "Группа команд" — зависит от выбранной категории + "+ Создать группу"

2. Логика для редактирования существующей команды:
   - Категория и группа предзаполнены (команда уже где-то лежит)
   - Пользователь может изменить размещение

3. Логика для новой команды:
   - Если команда есть в словаре → предложить категорию/группу из словаря
   - Визуальная индикация: зелёная обводка = рекомендуемое размещение
   - Если пользователь меняет на своё → жёлтая обводка

4. Создание новой категории/группы:
   - При выборе "+ Создать..." — показать inline input
   - После ввода и Enter — добавить в список и выбрать

5. Связь селектов:
   - При смене категории — обновить список групп
   - Если была выбрана группа, которой нет в новой категории — сбросить на первую

6. Стили:
   - Кастомные селекты (не нативные) для консистентности с дизайном
   - Или нативные с базовой стилизацией — на твоё усмотрение

Формат: обновлённые script.js, style.css, index.html.
```

**Модель:** Claude Sonnet 4

**Файлы для работы:**
- script.js
- style.css
- index.html
- commands.json
- commands-dictionary.json

**Результат:**
Работающие связанные селекты категория/группа

---

### Задача 5: Визуальная индикация полей

**Для тебя:**
Добавляем цветовую индикацию состояния полей: жёлтая обводка при фокусе/hover, зелёная когда значение совпадает с рекомендацией из словаря. Это помогает пользователю понять, использует ли он стандартные настройки или кастомные.

**Сложность:** 🟢 Легко

**Промт:**

```
Добавь визуальную индикацию состояния полей в окне редактирования Hotpaws.

Контекст: Цветовые обводки показывают состояние полей. Жёлтая = активное/hover. Зелёная = значение совпадает с рекомендацией из словаря.

Текущий style.css: [ВСТАВИТЬ]
Текущий script.js: [ВСТАВИТЬ]

Требования:

1. CSS классы для состояний:
   - .field-active — жёлтая обводка 2px (#FFDD00)
   - .field-recommended — зелёная обводка 2px (#4CAF50)
   - .field-custom — жёлтая обводка (пользовательское значение)
   - .field-conflict — красная обводка 2px (#F44336) — для будущих хоткеев

2. Применение:
   - :focus и :hover на input/select/textarea → жёлтая обводка
   - Поля "Категория" и "Группа": зелёная если совпадает со словарём
   - Поле "Команда": зелёная если команда есть в словаре

3. JavaScript логика:
   - updateFieldIndicators() — вызывается при изменении любого поля
   - Сравнивает текущие значения со словарём
   - Добавляет/убирает классы

4. Анимация:
   - Плавный переход цвета обводки (transition: border-color 0.2s)

5. Не применять индикацию:
   - К кнопкам
   - К полю "Описание" (там нет понятия "рекомендация")

Формат: обновлённые style.css и script.js.
```

**Модель:** Claude Haiku 4.5

**Файлы для работы:**
- style.css
- script.js

**Результат:**
Визуальная индикация состояния полей

---

### Задача 6: Кнопки "Сохранить" и "Удалить"

**Для тебя:**
Добавляем кнопки действий в модальное окно. "Сохранить" — сохраняет изменения и закрывает окно. "Удалить" — показывает подтверждение перед удалением.

**Сложность:** 🟢 Легко

**Промт:**

```
Добавь кнопки "Сохранить" и "Удалить" в окно редактирования Hotpaws.

Контекст: Внизу модального окна две кнопки. "Удалить" требует подтверждения.

Текущий script.js: [ВСТАВИТЬ]
Текущий style.css: [ВСТАВИТЬ]
Текущий index.html: [ВСТАВИТЬ]

Требования:

1. Кнопка "Сохранить":
   - Первичная кнопка (яркая, акцентная)
   - При клике: валидация → сохранение → закрытие окна
   - Валидация: поле "Команда" обязательно
   - Если поле пустое — подсветить красным, не закрывать

2. Кнопка "Удалить":
   - Вторичная кнопка (менее заметная, красноватая)
   - При клике — показать диалог подтверждения

3. Диалог подтверждения удаления:
   - Мини-модал поверх основного
   - Текст: "Удалить команду «{название}»?"
   - Кнопки: "Да, удалить" (красная) и "Ой, нет!" (обычная)
   - "Да, удалить" → удалить команду из commands.json → закрыть оба окна
   - "Ой, нет!" → просто закрыть диалог подтверждения

4. Сохранение команды:
   - Обновить объект в памяти (commandsData)
   - Отправить в Swift для записи в файл
   - webkit.messageHandlers.saveCommands.postMessage(commandsData)

5. Удаление команды:
   - Удалить из объекта в памяти
   - Отправить обновлённые данные в Swift
   - Перерендерить интерфейс

6. Swift часть:
   - Хендлер "saveCommands" — записывает в ~/.hotpaws/commands.json

Формат: обновлённые script.js, style.css, index.html, main.swift.
```

**Модель:** Claude Sonnet 4

**Файлы для работы:**
- script.js
- style.css
- index.html
- main.swift

**Результат:**
Работающие кнопки сохранения и удаления

---

### Задача 7: Логика закрытия окна с проверкой изменений

**Для тебя:**
Реализуем умное закрытие модального окна. Если были изменения — спрашиваем что делать. Если изменений не было — просто закрываем.

**Сложность:** 🟡 Средне

**Промт:**

```
Добавь логику закрытия окна с проверкой изменений в Hotpaws.

Контекст: При закрытии окна редактирования проверяем, были ли изменения. Если да — показываем диалог.

Текущий script.js: [ВСТАВИТЬ]
Текущий style.css: [ВСТАВИТЬ]

Требования:

1. Отслеживание изменений:
   - При открытии окна — сохранить исходное состояние (originalData)
   - При каждом изменении поля — сравнивать с оригиналом
   - Функция hasUnsavedChanges() → boolean

2. Способы закрытия:
   - Клик вне модального окна (на backdrop)
   - Нажатие Esc
   - Крестик в углу (если есть) — опционально

3. Логика при закрытии:
   - Если hasUnsavedChanges() === false → просто закрыть
   - Если есть изменения → показать диалог

4. Диалог "Несохранённые изменения":
   - Текст: "Сохранить изменения перед выходом?"
   - Кнопки:
     - "Да, сохранить и выйти" → сохранить + закрыть
     - "Просто уйти" → закрыть без сохранения  
     - "Ой, вернуться" → закрыть диалог, остаться в редакторе

5. Стили диалога:
   - Похож на диалог удаления
   - Три кнопки в ряд или стопкой

6. Edge cases:
   - Если при валидации ошибка (пустая команда) — не закрывать
   - Если команда новая и все поля пустые — нет изменений, просто закрыть

Формат: обновлённые script.js и style.css.
```

**Модель:** Claude Sonnet 4

**Файлы для работы:**
- script.js
- style.css

**Результат:**
Умное закрытие окна с проверкой изменений

---

### Задача 8: Создание новой команды

**Для тебя:**
Добавляем возможность создать новую команду. Кнопка "+" появляется в режиме редактирования. При создании — открывается пустое окно редактирования с предзаполнением из словаря (если команда известна).

**Сложность:** 🟡 Средне

**Промт:**

```
Добавь создание новой команды в Hotpaws.

Контекст: В режиме редактирования появляется кнопка "+" для создания новой команды. Новая команда добавляется в выбранную категорию и группу.

Текущий script.js: [ВСТАВИТЬ]
Текущий style.css: [ВСТАВИТЬ]
Текущий index.html: [ВСТАВИТЬ]

Требования:

1. Кнопка добавления:
   - В режиме редактирования: кнопка "+" в конце списка команд каждой группы
   - Или: одна большая кнопка "Добавить команду" внизу контента
   - Выбери более удобный UX

2. Открытие окна для новой команды:
   - openCommandEditor(null) — без данных = новая команда
   - Заголовок окна: "Новая команда"
   - Все поля пустые
   - Категория и группа — предвыбраны текущие (откуда нажали "+")

3. Логика предзаполнения из словаря:
   - Пользователь вводит команду → автодополнение как обычно
   - Когда команда совпадает со словарём:
     - Автозаполнить "Описание" (если пустое)
     - Предложить "Категорию" и "Группу" из словаря
     - Показать зелёную обводку на предложенных полях

4. Сохранение новой команды:
   - Валидация: команда обязательна
   - Добавить в выбранную категорию и группу
   - Если категория/группа новые — создать их
   - Сохранить в commands.json

5. ID новой команды:
   - Генерировать уникальный ID (timestamp или uuid)

Формат: обновлённые script.js, style.css, index.html.
```

**Модель:** Claude Sonnet 4

**Файлы для работы:**
- script.js
- style.css
- index.html

**Результат:**
Возможность создавать новые команды

---

## Фаза 2: Drag & Drop и структура

---

### Задача 9: Inline-редактирование названий категорий

**Для тебя:**
Добавляем возможность переименовать категорию кликом по ней в режиме редактирования. Работает как переименование файла в Finder: клик → поле ввода → Enter или клик вне → сохранить.

**Сложность:** 🟡 Средне

**Промт:**

```
Добавь inline-редактирование названий категорий в Hotpaws.

Контекст: В режиме редактирования клик по названию категории (табу) превращает его в поле ввода. Изменение сохраняется автоматически.

Текущий script.js: [ВСТАВИТЬ]
Текущий style.css: [ВСТАВИТЬ]
Текущий index.html: [ВСТАВИТЬ]

Требования:

1. Активация редактирования:
   - В режиме редактирования: клик по табу категории
   - Таб превращается в input с текущим названием
   - Текст выделен (select all) для быстрой замены
   - Автофокус на input

2. Сохранение:
   - Enter → сохранить и выйти из режима редактирования названия
   - Клик вне input → сохранить и выйти
   - Esc → отменить изменения, вернуть старое название

3. Валидация:
   - Название не может быть пустым
   - Если пустое при сохранении → вернуть старое

4. Автосохранение:
   - После изменения названия — сразу сохранить commands.json
   - Не требует нажатия отдельной кнопки "Сохранить"

5. Визуал:
   - Input стилизован как таб, но с обводкой редактирования
   - Плавный переход таб → input

6. Обновление интерфейса:
   - После сохранения — обновить название во всех местах
   - Не перерендеривать весь список команд

Формат: обновлённые script.js, style.css.
```

**Модель:** Claude Sonnet 4

**Файлы для работы:**
- script.js
- style.css
- index.html

**Результат:**
Inline-редактирование категорий

---

### Задача 10: Inline-редактирование названий групп

**Для тебя:**
Аналогично категориям — редактирование названий групп команд. Клик по заголовку группы → поле ввода.

**Сложность:** 🟢 Легко

**Промт:**

```
Добавь inline-редактирование названий групп команд в Hotpaws.

Контекст: Работает аналогично категориям. Клик по заголовку группы → редактирование.

Текущий script.js: [ВСТАВИТЬ]
Текущий style.css: [ВСТАВИТЬ]

Требования:

1. Использовать ту же логику что для категорий:
   - Клик по .group-title в режиме редактирования
   - Превращается в input
   - Enter/blur → сохранить
   - Esc → отменить

2. Создать переиспользуемую функцию:
   - makeEditable(element, onSave, onCancel)
   - Использовать и для категорий, и для групп

3. Автосохранение:
   - После изменения — сохранить commands.json

Формат: обновлённый script.js (с рефакторингом если нужно).
```

**Модель:** Claude Sonnet 4

**Файлы для работы:**
- script.js
- style.css

**Результат:**
Inline-редактирование групп + рефакторинг

---

### Задача 11: Создание новой категории ("+ Категория")

**Для тебя:**
Кнопка "+" справа от последней категории. При клике — добавляется новая категория, сразу в режиме редактирования названия.

**Сложность:** 🟢 Легко

**Промт:**

```
Добавь кнопку создания новой категории в Hotpaws.

Контекст: В режиме редактирования справа от табов категорий появляется кнопка "+". При клике создаётся новая категория.

Текущий script.js: [ВСТАВИТЬ]
Текущий style.css: [ВСТАВИТЬ]
Текущий index.html: [ВСТАВИТЬ]

Требования:

1. Кнопка "+ Категория":
   - Появляется только в режиме редактирования
   - Справа от последнего таба
   - Стиль: менее заметная чем обычные табы, но кликабельная

2. При клике:
   - Создать новую категорию с временным названием "Новая категория"
   - Добавить таб
   - Сразу активировать inline-редактирование названия
   - Создать одну пустую группу "Команды" внутри

3. Отмена создания:
   - Если при редактировании названия нажать Esc и название пустое
   - Или если название осталось "Новая категория" и в категории нет команд
   - Можно не удалять автоматически (на усмотрение)

4. Сохранение:
   - После ввода названия — сохранить в commands.json

Формат: обновлённые script.js, style.css, index.html.
```

**Модель:** Claude Sonnet 4

**Файлы для работы:**
- script.js
- style.css
- index.html

**Результат:**
Кнопка создания новой категории

---

### Задача 12: Drag & Drop команд

**Для тебя:**
Самая сложная задача фазы. Реализуем перетаскивание команд между группами и внутри группы. Кастомная реализация через mouse events для лучшего контроля над визуалом.

**Сложность:** 🔴 Сложно

**Промт:**

```
Реализуй Drag & Drop для команд в Hotpaws.

Контекст: В режиме редактирования команды можно перетаскивать мышью. Перенос между группами и внутри группы. Кастомная реализация (не HTML5 Drag API).

Текущий script.js: [ВСТАВИТЬ]
Текущий style.css: [ВСТАВИТЬ]

Требования:

1. Начало перетаскивания:
   - Только в режиме редактирования
   - Mousedown на команде + небольшое движение (5px) → начать drag
   - Создать "призрак" — полупрозрачную копию элемента
   - Оригинал остаётся на месте но с пониженной opacity

2. Во время перетаскивания:
   - Призрак следует за курсором
   - Определять позицию вставки (перед/после какой команды)
   - Показывать "placeholder" — горизонтальная линия или пустой блок
   - Placeholder появляется плавно (transition)

3. Drop zones:
   - Между командами в любой группе
   - В пустую группу
   - В другую категорию (если таб категории под курсором — переключить)

4. Завершение:
   - Mouseup → вставить команду в новую позицию
   - Если позиция не изменилась — ничего не делать
   - Обновить commands.json
   - Плавная анимация "приземления"

5. Отмена:
   - Esc во время drag → отменить, вернуть как было
   - Drop за пределами валидной зоны → отменить

6. Визуал:
   - Призрак: opacity 0.7, небольшая тень, scale 1.02
   - Placeholder: 2px линия акцентного цвета или полупрозрачный блок
   - Курсор: grabbing во время drag

7. Производительность:
   - Throttle для mousemove (каждые 16ms = 60fps)
   - Не перерендеривать весь список во время drag

Формат: обновлённые script.js и style.css с подробными комментариями.
```

**Модель:** Claude Opus 4.1

**Файлы для работы:**
- script.js
- style.css

**Результат:**
Полноценный Drag & Drop команд

---

### Задача 13: Drag & Drop категорий и групп

**Для тебя:**
Расширяем систему drag & drop на категории и группы. Категории можно менять местами перетаскиванием табов. Группы — перетаскиванием заголовков.

**Сложность:** 🔴 Сложно

**Промт:**

```
Добавь Drag & Drop для категорий и групп в Hotpaws.

Контекст: Расширяем существующий drag & drop. Категории и группы тоже можно перетаскивать для изменения порядка.

Текущий script.js с drag & drop команд: [ВСТАВИТЬ]
Текущий style.css: [ВСТАВИТЬ]

Требования:

1. Drag & Drop категорий (табов):
   - Mousedown на табе + движение → начать drag
   - Визуал: таб-призрак следует за курсором
   - Placeholder между табами
   - Можно менять порядок категорий

2. Drag & Drop групп:
   - Mousedown на заголовке группы + движение → начать drag
   - Визуал: вся группа (заголовок + команды) как призрак
   - Можно перемещать между категориями
   - Можно менять порядок внутри категории

3. Различение типов drag:
   - Определять что перетаскиваем: команду, группу или категорию
   - Разные placeholder для разных типов
   - Не допускать невалидные комбинации (группу в группу)

4. Сохранение:
   - После любого drop — автосохранение commands.json

5. Рефакторинг:
   - Создать абстракцию DragManager
   - Конфигурируемые типы draggable элементов
   - Переиспользуемая логика

Формат: обновлённые script.js и style.css.
```

**Модель:** Claude Opus 4.1

**Файлы для работы:**
- script.js
- style.css

**Результат:**
Полная система drag & drop для всех элементов

---

### Задача 14: Автосохранение профиля

**Для тебя:**
Собираем всё вместе: автосохранение при любых изменениях в режиме редактирования (кроме модального окна, где есть кнопка "Сохранить"). Добавляем debounce чтобы не записывать файл слишком часто.

**Сложность:** 🟡 Средне

**Промт:**

```
Реализуй автосохранение профиля в Hotpaws.

Контекст: В режиме редактирования изменения сохраняются автоматически (кроме модального окна редактирования команды).

Текущий script.js: [ВСТАВИТЬ]
Текущий main.swift: [ВСТАВИТЬ]

Требования:

1. Что сохраняется автоматически:
   - Изменение названия категории
   - Изменение названия группы
   - Перемещение команд (drag & drop)
   - Перемещение групп
   - Перемещение категорий
   - Создание категории/группы

2. Что сохраняется по кнопке "Сохранить":
   - Редактирование команды в модальном окне

3. Механизм автосохранения:
   - Функция scheduleAutosave()
   - Debounce: сохранять не чаще чем раз в 500ms
   - При закрытии режима редактирования — сохранить немедленно

4. Индикация сохранения:
   - Маленькая иконка или текст "Сохранено" / "Сохранение..."
   - Появляется в углу на 1-2 секунды после сохранения

5. Swift часть:
   - Убедиться что хендлер saveCommands работает надёжно
   - Обработка ошибок записи файла

6. Восстановление при ошибке:
   - Если запись не удалась — показать ошибку
   - Не потерять изменения в памяти

Формат: обновлённые script.js и main.swift.
```

**Модель:** Claude Sonnet 4

**Файлы для работы:**
- script.js
- main.swift

**Результат:**
Надёжное автосохранение с индикацией

---

## Фаза 3: Алиасы

---

### Задача 15: Генерация файла aliases.sh

**Для тебя:**
Создаём систему генерации файла `~/.hotpaws/aliases.sh`. Файл содержит все алиасы команд в формате bash/zsh.

**Сложность:** 🟡 Средне

**Промт:**

```
Создай систему генерации aliases.sh для Hotpaws.

Контекст: Алиасы пользователя хранятся в commands.json (поле alias у команды). При сохранении генерируется файл ~/.hotpaws/aliases.sh.

Текущий main.swift: [ВСТАВИТЬ]
Текущий ConfigManager.swift: [ВСТАВИТЬ]

Требования:

1. Формат aliases.sh:
   ```bash
   # Hotpaws Aliases — автогенерируемый файл
   # Не редактируйте вручную, изменения будут перезаписаны
   # Сгенерировано: 2024-01-15 12:30:00
   
   alias пинг='ping -c 4 google.com'
   alias статус='git status'
   # ... остальные алиасы
   ```

2. Генерация:
   - Функция generateAliasesFile() в Swift
   - Вызывается после каждого сохранения commands.json
   - Читает все команды, фильтрует имеющие alias

3. Формат алиаса:
   - alias NAME='COMMAND'
   - Экранирование одинарных кавычек в команде (replace ' with '\'' )
   - Поддержка кириллицы в названии алиаса

4. Обновление commands.json:
   - Добавить поле "alias" в структуру команды
   - Опциональное, строка или null

5. Порядок алиасов:
   - Сортировать по имени алиаса (alphabetically)
   - Сначала латиница, потом кириллица

Формат: обновлённые main.swift и ConfigManager.swift.
```

**Модель:** Claude Sonnet 4

**Файлы для работы:**
- main.swift
- ConfigManager.swift

**Результат:**
Генерация файла aliases.sh

---

### Задача 16: Интеграция с .zshrc/.bashrc

**Для тебя:**
Добавляем строку подгрузки aliases.sh в конфиги шеллов. Делаем это аккуратно: проверяем наличие файлов, не дублируем строки, показываем запрос разрешения.

**Сложность:** 🔴 Сложно

**Промт:**

```
Реализуй интеграцию Hotpaws с .zshrc и .bashrc.

Контекст: При первом создании алиаса нужно добавить строки в конфиги шеллов для подгрузки aliases.sh.

Текущий main.swift: [ВСТАВИТЬ]
Текущий ConfigManager.swift: [ВСТАВИТЬ]

Требования:

1. Что добавляем в конфиги:
   ```bash
   # Hotpaws aliases
   [ -f ~/.hotpaws/aliases.sh ] && source ~/.hotpaws/aliases.sh && print -P '%F{green}🐾 Hotpaws aliases loaded!'
   ```
   (для bash заменить print -P на echo -e)

2. Какие файлы проверяем:
   - ~/.zshrc
   - ~/.bashrc
   - ~/.bash_profile (если .bashrc нет)
   - Добавляем во все существующие

3. Логика:
   - Проверить есть ли уже наша строка (искать "hotpaws" или ".hotpaws/aliases")
   - Если нет — показать диалог запроса разрешения
   - Если пользователь разрешил — добавить в конец файла
   - Создать бэкап перед изменением (.zshrc.hotpaws-backup)

4. Диалог разрешения (в Swift):
   - NSAlert с текстом: "Hotpaws хочет добавить загрузку алиасов в ваш .zshrc/.bashrc"
   - Кнопки: "Разрешить" / "Не сейчас" / "Никогда не спрашивать"
   - Сохранить выбор в UserDefaults

5. Когда спрашивать:
   - При первом сохранении команды с алиасом
   - Только один раз (или если файл конфига изменился)

6. Обработка ошибок:
   - Нет прав на запись → показать инструкцию как сделать вручную
   - Файл заблокирован → повторить позже

Формат: обновлённые main.swift и ConfigManager.swift с диалогами.
```

**Модель:** Claude Opus 4.5

**Файлы для работы:**
- main.swift
- ConfigManager.swift

**Результат:**
Безопасная интеграция с конфигами шеллов

---

### Задача 17: Поле "Alias" в окне редактирования

**Для тебя:**
Добавляем поле "Alias" в модальное окно редактирования команды. При сохранении алиас добавляется в aliases.sh.

**Сложность:** 🟡 Средне

**Промт:**

```
Добавь поле "Alias" в окно редактирования команды Hotpaws.

Контекст: Пользователь может задать короткое имя (алиас) для команды. Алиас будет доступен в терминале.

Текущий script.js: [ВСТАВИТЬ]
Текущий style.css: [ВСТАВИТЬ]
Текущий index.html: [ВСТАВИТЬ]

Требования:

1. Поле "Alias":
   - Input type="text"
   - Placeholder: "например: статус"
   - Опциональное поле
   - Подсказка под полем: "Будет доступно в терминале как команда"

2. Валидация:
   - Только буквы (латиница + кириллица), цифры, дефис, подчёркивание
   - Без пробелов
   - Не начинается с цифры
   - Показывать ошибку если невалидный формат

3. Проверка уникальности:
   - Алиас не должен совпадать с другими алиасами в commands.json
   - Если совпадает — жёлтая обводка + предупреждение
   - Можно сохранить (перезапишет старый)

4. Визуальная индикация:
   - После поля показать превью: `alias {название}='{команда}'`
   - Обновляется в реальном времени при вводе

5. При сохранении:
   - Добавить alias в объект команды
   - Триггернуть перегенерацию aliases.sh

Формат: обновлённые script.js, style.css, index.html.
```

**Модель:** Claude Sonnet 4

**Файлы для работы:**
- script.js
- style.css
- index.html

**Результат:**
Работающее поле алиаса с валидацией

---

## Фаза 4: Глобальные хоткеи команд

---

### Задача 18: Регистрация глобальных хоткеев команд

**Для тебя:**
Система регистрации множества глобальных хоткеев — по одному на каждую команду с заданным хоткеем. При старте приложения читаем commands.json и регистрируем все хоткеи.

**Сложность:** 🔴 Сложно

**Промт:**

```
Реализуй систему глобальных хоткеев для команд в Hotpaws.

Контекст: Каждая команда может иметь свой глобальный хоткей. При нажатии — команда выполняется в терминале без открытия оверлея.

Текущий main.swift: [ВСТАВИТЬ]

Требования:

1. Структура данных:
   - В commands.json поле "hotkey" у команды
   - Формат: { "modifiers": ["cmd", "shift"], "key": "g" }
   - Или строка: "cmd+shift+g"

2. HotkeyManager:
   - Класс для управления хоткеями команд
   - registerCommandHotkey(commandId, hotkey, action)
   - unregisterCommandHotkey(commandId)
   - updateAllHotkeys() — перечитать из commands.json

3. При старте приложения:
   - Загрузить commands.json
   - Зарегистрировать все хоткеи с командами
   - F19 остаётся для оверлея

4. При нажатии хоткея:
   - Найти команду по ID
   - Выполнить в терминале (через существующий механизм)
   - Не открывать оверлей

5. При изменении commands.json:
   - Перерегистрировать хоткеи
   - Удалить старые, добавить новые

6. Конфликты:
   - Если два хоткея одинаковые — последний перезаписывает
   - Хоткей команды перебивает системный (как Raycast)

7. Лимиты:
   - Максимум 50 активных хоткеев (для производительности)
   - При превышении — предупреждение

Формат: новый HotkeyManager.swift + обновлённый main.swift.
```

**Модель:** Claude Opus 4.5

**Файлы для работы:**
- main.swift

**Результат:**
HotkeyManager.swift + интеграция

---

### Задача 19: Поле "Горячая клавиша" в UI

**Для тебя:**
Добавляем поле захвата горячей клавиши в окно редактирования. Пользователь кликает на поле и нажимает комбинацию — она записывается.

**Сложность:** 🔴 Сложно

**Промт:**

```
Добавь поле "Горячая клавиша" в окно редактирования Hotpaws.

Контекст: Поле для захвата комбинации клавиш. Клик → ожидание ввода → отображение комбинации.

Текущий script.js: [ВСТАВИТЬ]
Текущий style.css: [ВСТАВИТЬ]

Требования:

1. Поле захвата хоткея:
   - Не обычный input, а кастомный элемент
   - Состояния: пустое / ожидание ввода / заполнено
   - В режиме ожидания: "Нажмите комбинацию..."

2. Захват комбинации:
   - Клик по полю → режим ожидания
   - keydown → записать modifiers + key
   - Отобразить красиво: "⌘⇧G" или "Cmd+Shift+G"
   - Esc → отменить, вернуть предыдущее значение

3. Валидация:
   - Хоткей должен содержать хотя бы один modifier (Cmd/Ctrl/Alt/Shift)
   - Одиночные буквы без модификаторов — не принимать
   - F-клавиши можно без модификаторов (кроме F19 — занята)

4. Проверка конфликтов (внутри Hotpaws):
   - Проверить нет ли такого хоткея у другой команды
   - Если есть — красная обводка + предупреждение
   - "Эта комбинация используется для «{название команды}»"
   - Можно сохранить — перезапишет старый

5. Очистка хоткея:
   - Кнопка "×" рядом с полем или Backspace в режиме ожидания
   - Удаляет хоткей с команды

6. Данные:
   - Сохранять в формате { modifiers: [], key: "" }
   - Или строкой "cmd+shift+g" — выбери удобнее

Формат: обновлённые script.js и style.css.
```

**Модель:** Claude Opus 4.1

**Файлы для работы:**
- script.js
- style.css

**Результат:**
Поле захвата хоткеев с валидацией

---

### Задача 20: Выполнение команды по глобальному хоткею

**Для тебя:**
Связываем всё вместе: хоткей нажат → Swift получает событие → выполняем команду в терминале. Без открытия оверлея.

**Сложность:** 🟡 Средне

**Промт:**

```
Реализуй выполнение команды по глобальному хоткею в Hotpaws.

Контекст: При нажатии зарегистрированного хоткея — выполнить связанную команду в терминале без открытия оверлея.

Текущий main.swift: [ВСТАВИТЬ]
Текущий HotkeyManager.swift: [ВСТАВИТЬ]

Требования:

1. Flow:
   - Пользователь нажимает хоткей (например Cmd+Shift+G)
   - HotkeyManager получает событие
   - Находит команду по хоткею
   - Вызывает executeInTerminal(command)
   - Активирует терминал

2. Режим выполнения:
   - Использовать текущий режим (Insert and Execute / Insert Only)
   - Или: хоткей команды всегда Execute (на усмотрение)

3. Feedback пользователю:
   - Опционально: короткая нотификация "Выполнено: git status"
   - Или просто активировать терминал

4. Edge cases:
   - Если терминал не открыт — открыть
   - Если команда содержит переменные (типа $1) — вставить как есть

5. Тестирование:
   - Добавить тестовую команду с хоткеем
   - Проверить что работает из любого приложения

Формат: обновлённые main.swift и HotkeyManager.swift.
```

**Модель:** Claude Opus 4.5

**Файлы для работы:**
- main.swift
- HotkeyManager.swift

**Результат:**
Работающие глобальные хоткеи команд

---

## Порядок выполнения

```
Фаза 0: Подготовка
└── [0] Словарь команд

Фаза 1: Базовое редактирование
├── [1] Вход/выход из режима
├── [2] Модальное окно
├── [3] Автодополнение команд (зависит от 0)
├── [4] Селекты категория/группа (зависит от 0)
├── [5] Визуальная индикация
├── [6] Кнопки сохранить/удалить
├── [7] Логика закрытия окна
└── [8] Создание новой команды

Фаза 2: Drag & Drop
├── [9] Inline-редактирование категорий
├── [10] Inline-редактирование групп
├── [11] Кнопка "+ Категория"
├── [12] Drag & Drop команд
├── [13] Drag & Drop категорий/групп
└── [14] Автосохранение

Фаза 3: Алиасы
├── [15] Генерация aliases.sh
├── [16] Интеграция с .zshrc/.bashrc
└── [17] Поле "Alias"

Фаза 4: Хоткеи
├── [18] Регистрация хоткеев
├── [19] Поле "Горячая клавиша"
└── [20] Выполнение по хоткею
```

---

## Быстрый старт: минимальный функционал

Для быстрого результата:
1. Задача 0 — словарь (можно параллельно)
2. Задача 1 — вход в режим
3. Задача 2 — модальное окно
4. Задача 6 — кнопки
5. Задача 8 — создание команды

После этого уже можно редактировать и создавать команды!

---

#hotpaws #edit-mode #tasks
