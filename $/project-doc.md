# # 📋 Hotpaws — Проектная документация

> Шаблон для работы с Claude в отдельных чатах. Скопируй этот документ в Bear и заполни.

---

## 1. Project Overview

**Название:** Hotpaws (Горячие лапки)

**Одной строкой:** macOS приложение-оверлей с подсказками по терминальным командам, позволяет вставлять команды в терминал одним кликом.

**Для кого:** Новички в программировании, которые не могут запомнить bash-команды.

**Ключевая проблема:** Терминальные команды сложно запомнить, приходится постоянно гуглить.

**Решение:** Горячая клавиша вызывает красивый оверлей с категоризированными командами. Клик по команде — она летит в терминал.

---

## 2. Архитектура

### Диаграмма потока данных
```
[F19 нажата] → [Swift AppDelegate]
                    ↓
              [WKWebView окно]
                    ↓
              [HTML + JS интерфейс]
                    ↓ (клик по команде)
              [webkit.messageHandlers]
                    ↓
              [Swift получает команду]
                    ↓
              [AppleScript → Terminal/iTerm2]
```

### Описание компонентов

| Компонент | Ответственность | Зависимости |
|-----------|-----------------|-------------|
| AppDelegate | Горячие клавиши, жизненный цикл окна | - |
| WKWebView | Отображение UI | HTML, JS, CSS |
| JavaScript | Логика интерфейса, отправка команд | webkit.messageHandlers |
| ConfigManager | Чтение/запись конфигов | FileManager |
| AppleScript | Вставка команд в терминал | Terminal.app / iTerm2 |

### Принципы
- Минимум кода — максимум пользы
- Весь дизайн вынесен в CSS (редактируется пользователем)
- Конфиги в ~/.hotpaws/ (легко бэкапить и синхронизировать)
- Без создания алиасов — просто вставка текста

---

## 3. Технический стек

### Платформа
- **OS:** macOS 12+
- **Язык:** Swift 5
- **UI:** WKWebView (HTML + CSS + JavaScript)

### Сборка
- **Метод:** swiftc (без Xcode)
- **Команда:** `./build.sh`

### Зависимости
- WebKit (встроен в macOS)
- Carbon (для глобальных горячих клавиш)
- Никаких внешних зависимостей!

### Окружение разработки
- **IDE:** VS Code / Xcode (опционально)
- **Путь проекта:** `/Users/ufoanima/Dev/experiments/hotpaws/`

---

## 4. Структура файлов

### Проект (исходники)
```
hotpaws/
├── main-webview.swift      # Точка входа, AppDelegate, WKWebView
├── ConfigManager.swift     # Загрузка конфигов из ~/.hotpaws/
├── index.html              # HTML структура оверлея
├── script.js               # JavaScript логика интерфейса
├── style.css               # CSS стили (дефолтные)
├── commands.json           # Дефолтные команды
├── build.sh                # Скрипт компиляции
├── Info.plist              # Конфиг приложения
└── AppIcon.icns            # Иконка
```

### Пользовательские файлы (создаются при первом запуске)
```
~/.hotpaws/
├── commands.json    # Команды пользователя
├── style.css        # Кастомные стили
├── aliases.json     # Алиасы (будущее)
└── config.json      # Настройки (будущее)
```

---

## 5. Текущее состояние

### ✅ Работает (в старой версии)
- [ ] Оверлей появляется по F19
- [ ] Категории и группы команд отображаются
- [ ] Клик по команде отправляет её в терминал
- [ ] ESC закрывает оверлей
- [ ] Стили загружаются из ~/.hotpaws/style.css

### ⚠️ Частично работает
- [ ] Выбор терминала — только через код

### ❌ Не реализовано
- [ ] Режим "зажать" (оверлей виден пока зажата клавиша)
- [ ] UI для редактирования команд
- [ ] Алиасы
- [ ] Навигация стрелками ← →

### 🐛 Известные баги
- [ ] Требует Accessibility permissions (нет нормального онбординга)
- [ ] [добавь свои]

---

## 6. Roadmap / Задачи

### Фаза 1: MVP (чистый перезапуск)
- [ ] Базовое окно с WKWebView
- [ ] Глобальная горячая клавиша F19
- [ ] Загрузка commands.json
- [ ] Рендер команд в HTML
- [ ] Клик → отправка в терминал
- [ ] ESC → закрыть

### Фаза 2: Удобства
- [ ] Режим "зажать"
- [ ] Навигация стрелками между категориями
- [ ] Выбор терминала в меню
- [ ] Онбординг для Accessibility

### Фаза 3: Расширения
- [ ] UI для редактирования команд
- [ ] Поиск по командам
- [ ] Алиасы

### Backlog (когда-нибудь)
- [ ] iCloud синхронизация
- [ ] Темы оформления
- [ ] Экспорт/импорт конфигов

---

## 7. Контракты между компонентами

### Формат данных: commands.json
```json
{
  "version": "1.0",
  "categories": [
    {
      "id": "git",
      "name": "Git",
      "icon": "🌿",
      "groups": [
        {
          "name": "Базовое",
          "commands": [
            {
              "label": "Статус",
              "command": "git status",
              "description": "Показать состояние репозитория"
            }
          ]
        }
      ]
    }
  ]
}
```

### JS → Swift сообщения
```javascript
// Выполнить команду в терминале
webkit.messageHandlers.executeCommand.postMessage({
    command: "git status"
});

// Закрыть оверлей
webkit.messageHandlers.closeOverlay.postMessage({});
```

### Swift WKScriptMessageHandler
```swift
func userContentController(_ userContentController: WKUserContentController, 
                          didReceive message: WKScriptMessage) {
    if message.name == "executeCommand" {
        // Обработка команды
    }
}
```

### AppleScript для Terminal.app
```applescript
tell application "Terminal"
    activate
    do script "COMMAND" in front window
end tell
```

### AppleScript для iTerm2
```applescript
tell application "iTerm"
    tell current session of current window
        write text "COMMAND"
    end tell
end tell
```

---

## 8. Глоссарий

| Термин | Значение |
|--------|----------|
| Оверлей | Полноэкранное полупрозрачное окно поверх всех приложений |
| Категория | Верхний уровень группировки (Git, Docker, etc.) |
| Группа | Подгруппа внутри категории (Базовое, Ветки, etc.) |
| Команда | Конкретная терминальная команда с описанием |

---

## 9. Ссылки

- **Старый проект:** `/Users/ufoanima/Dev/archived/hotpaws/`
- **Новый проект:** `/Users/ufoanima/Dev/experiments/hotpaws/` (создать)
- **README:** см. вложение в первом чате

---

## 10. Как использовать этот документ

При создании нового чата с Claude:
1. Скопируй релевантные секции (не весь документ)
2. Укажи конкретную задачу
3. Приложи нужные файлы кода

**Пример начала чата:**
```
Контекст: Hotpaws — macOS приложение, оверлей с командами для терминала.
Стек: Swift 5, WKWebView, AppleScript.
Текущая задача: [конкретная задача]
Релевантный код: [приложить файл]
```

---

#hotpaws #documentation #project
